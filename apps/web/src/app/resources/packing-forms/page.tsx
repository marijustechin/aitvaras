import { PackingFormsPage } from "@/features/manage-packing-forms";
import { AppShell } from "@/widgets/app-shell";

export default function Page() {
  return (
    <AppShell>
      <PackingFormsPage />
    </AppShell>
  );
}
